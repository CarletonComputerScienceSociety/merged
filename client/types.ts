export interface Event {
    id: number;
    title: string;
    body: string;
    start: string;
    end: string;
    category: string;
}

export interface Organization {
    id: number;
    title: string;
    description: string;
}
